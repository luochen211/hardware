# 硬件是什么：领域与系统导览

先用常见的三层模型认识一个联网硬件作品：感知层负责采集物理状态、进行本地控制和执行动作；网络层传递数据和命令；应用层负责展示信息、让人操作或运行更高层的逻辑。

## 一张图认识硬件

```mermaid
mindmap
  root((联网硬件作品))
    感知层（设备端）
      传感器感知
      单片机控制
      电机或水泵执行
    网络层
      串口
      蓝牙
      Wi-Fi
    应用层
      手机或网页
      智能体
      查看状态与下发指令
```

## 三层之间怎样传递信息

```mermaid
flowchart LR
  subgraph D[感知层：传感、控制与执行]
    S[传感器] --> M[单片机与固件]
    M --> X[电机、灯或水泵]
  end
  subgraph N[网络层：传递数据和命令]
    L[串口 / 蓝牙 / Wi-Fi]
  end
  subgraph A[应用层：查看和操作]
    U[手机 / 网页 / 智能体]
  end
  M -->|设备主动上报的数据| L
  L --> U
  U -->|用户或程序发出的命令| L
  L --> M
```

设备把测量值和状态发到应用，应用也能沿着网络把命令发回设备。一个会避障的小车可以在感知层读取距离并本地控制电机；蓝牙只传状态和遥控命令。自动浇水装置则可以根据土壤湿度读数触发水泵。

## 硬件和软件的一个关键区别

软件代理能检查的是它有权限访问的软件状态；硬件的物理状态不会自动变成电脑可见的信息。电脑连接硬件后，只能读取设备通过传感器和通信接口提供的内容。要知道设备真实状态，需要先有对应的传感器、接口和读取程序。

例如，串口监视器能显示 ESP32 发出的日志，但这不能单独证明传感器已经正确接线、供电正常或测量准确。硬件调试因此常要结合程序日志、接口读数和实际测量。

## 一套硬件系统怎样工作

不同设备虽然用途各异，常见工作链路可以概括为：

```text
需求与环境 → 传感器 → 控制器与固件 → 驱动与执行器 → 结果反馈
                         ↕
                   通信与界面
```

## 模块一：需求场景

先说清楚系统要解决的问题，以及怎样算完成。

```mermaid
mindmap
  root((需求场景))
    使用对象
    环境条件
    测量目标
    控制目标
    验收指标
```

**智能农业例子：** 测量盆土含水变化；低于设定范围时提示或浇水；用干土、湿土和阈值触发结果验收。

## 模块二：感知输入

传感器把温度、湿度、光照等物理量转换成控制器能够读取的信号。

```mermaid
mindmap
  root((感知输入))
    传感器
      电容式
      电阻式
      数字式
    信号接口
      模拟电压
      GPIO
      I2C或SPI
    数据处理
      ADC采样
      滤波
      校准换算
```

**要核实：** 传感器供电、输出电压范围、接线引脚、采样方式和干湿校准值。传感器接上了，不代表固件已经读取它。

## 模块三：计算控制

控制器读取输入、运行固件逻辑，再决定是否改变输出。

```mermaid
mindmap
  root((计算控制))
    控制器
      ESP32
      GPIO与外设
      存储与任务
    固件流程
      初始化
      周期采样
      数据换算
      状态判断
    控制策略
      阈值
      定时
      手动自动模式
    异常处理
      传感器断线
      越界读数
      安全停止
```

**当前实验对应：** ESP32 运行 `exp1_blink` 固件，初始化 SPI 并循环输出颜色状态；当前程序没有 ADC 采样或湿度判断。

## 模块四：输出执行

控制器的引脚通常不能直接带动大功率负载，需要驱动电路连接执行器。

```mermaid
mindmap
  root((输出执行))
    控制信号
      GPIO
      PWM
      SPI
    驱动电路
      MOSFET或三极管
      继电器
      电机驱动
    执行器
      WS2812灯
      水泵
      电磁阀
    负载保护
      续流与浪涌
      电流限制
      隔离
```

**当前实验对应：** WS2812 灯条是执行器，SPI 数据用于控制颜色。若加水泵，需要另行设计驱动和供电，不能把水泵直接接到 ESP32 GPIO。

## 模块五：连接基础

供电、接线和通信协议决定模块能不能稳定协同。

```mermaid
mindmap
  root((连接基础))
    供电
      电压
      峰值电流
      稳压
    电气连接
      引脚表
      共地
      电平兼容
    通信
      串口UART
      WiFi或蓝牙
      数据协议
    呈现
      串口监视器
      网页
      屏幕
```

**排错提示：** USB 串口已连接只说明电脑和板子的串口链路可用，不会自动生成湿度数据。网页只有收到并识别固件发送的数据，才能显示对应读数。

## 模块六：验证迭代

按层验证能更快定位问题，不要一开始就把传感器、执行器、网页和云端同时联调。

```mermaid
mindmap
  root((验证迭代))
    看设计
      原理图
      引脚定义
    单模块
      供电测量
      传感器原始值
      执行器动作
    联调
      串口协议
      网页解析
      端到端流程
    可靠性
      连续运行
      断线恢复
      环境防护
```

## 用这张图规划智能农业项目

建议按顺序扩展，每一步都有独立可见的结果：

1. 确认土壤湿度传感器型号、输出范围和 ESP32 ADC 引脚。
2. 只做 ADC 读取，在串口观察干土、湿土的原始值变化。
3. 用实测干湿值校准，再输出湿度百分比。
4. 约定串口或 Wi-Fi 数据格式，再让网页显示读数。
5. 最后加阈值判断、驱动模块和水泵，并验证断线与停止条件。

当前 WS2812 实验已经覆盖“计算控制、输出执行、串口观察”的一小段；湿度传感器采集和自动灌溉属于后续模块。详细过程与踩坑见[实验复盘](esp32-ws2812-lab-retrospective.md)。
